# path to saml files
SAML_FOLDER = '/home/joeg/lef/realme/MTS'

AUTH_METHOD = 'prd'  # fake, mts, its, prd

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'mydatabase',
        'USER': 'mydatabaseuser',
        'PASSWORD': 'mypassword',
        'HOST': '127.0.0.1',
        'PORT': '5432',
    }
}

